import { makeStyles } from '@rneui/themed';

const useStyles = makeStyles(() => ({
  head: {
    alignItems: 'center',
  },

  icon: {
    width: 200,
    height: 200,
  },
}));

export default useStyles;
